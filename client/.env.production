# Loaded automatically by `vite build` (production mode), overriding .env's
# absolute dev URL. Relative path works regardless of deploy domain because the
# server serves both the API and this built client from the same origin
# (see server's CLIENT_DIST_PATH — server/.env.example).
VITE_API_BASE_URL=/api
